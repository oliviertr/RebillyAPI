description: "The calculation instruction of scheduled time for payment"
type: object
properties:
  scheduleInstruction:
    $ref: "#/definitions/ScheduleMethod"
  amountPolicy:
    type: string
    enum:
      - "balance-outstanding"
      - "invoice-total"
required:
  - scheduleInstruction
  - amountPolicy
