allOf:
  - $ref: "#/definitions/ScheduleInstruction"
  - type: object
    properties:
      days:
        type: integer
      time:
        type: string
        format: time
    required:
      - days
