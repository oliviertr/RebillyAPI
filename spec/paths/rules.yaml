get:
  tags:
    - Rules
  summary: Retrieve a list of rules sets
  description: |
    Retrieve a list of rules sets
  parameters:
    - $ref: "#/parameters/collectionLimit"
    - $ref: "#/parameters/collectionOffset"
  responses:
    200:
      description: A list of Rules Sets was retrieved successfully
      schema:
        type: array
        items:
          $ref: "#/definitions/RuleSet"
    401:
      $ref: "#/responses/AccessForbidden"
