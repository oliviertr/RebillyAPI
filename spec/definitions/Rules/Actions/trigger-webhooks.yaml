description: ""
allOf:
  - $ref: "#/definitions/RuleAction"
  - type: object
    properties:
      webhooks:
        type: array
        items:
          $ref: "#/definitions/Webhook"
    required:
      - webhooks
