type: object
description: "The calculation instruction of scheduled time"
discriminator: method
properties:
  method:
    type: string
    enum:
      - "auto"
      - "immediately"
      - "duration-of-time"
      - "day-of-month"
      - "day-of-week"
      - "specific-time"
required:
  - method
