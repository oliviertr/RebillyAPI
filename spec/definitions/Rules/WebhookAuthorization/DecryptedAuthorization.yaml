type: object
discriminator: type
properties:
  type:
    type: string
    enum:
      - none
      - basic
      - digest
      - oauth1
      # Unsupported at the moment
      # - oauth2
required:
  - type
